{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm install express && EXPO_PUBLIC_API_URL=https://api.courtesyinspection.com/api npx expo export --platform web --output-dir web-build"
  },
  "deploy": {
    "startCommand": "node serve-web.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}